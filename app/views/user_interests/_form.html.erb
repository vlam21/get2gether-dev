<%= form_for(@user_interest) do |f| %>
  <% if @user_interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_interest.errors.count, "error") %> prohibited this user_interest from being saved:</h2>

      <ul>
      <% @user_interest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fbid %><br />
    <%= f.number_field :fbid %>
  </div>
  <div class="field">
    <%= f.label :interestid %><br />
    <%= f.number_field :interestid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
